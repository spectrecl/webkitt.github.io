<!DOCTYPE html>
<html lang="ru-RU">
	<head>
		<title>Купить адену на Lineage Classic</title>
		<meta charset="UTF-8">
		<meta name="description" content="У нас Вы можете купить адену Lineage 2 Classic RU | EU (классика) - Быстро и Дешево! Гарантии, Скидки, Бонусы! Доставка в течении 5 минут!">
		<meta name="keywords" content="купить адену classic lineage 2, Купить адену classic, купить адену 4game, купить адену классик, продажа адены на классике, Classic адена, адена классика">
		<link rel="shortcut icon" href="images/favicon.png" type="image/png">
		<link rel="stylesheet" href="css/style.css">
		<link rel="stylesheet" media="screen, projection" href="http://story-shop.ru/catalog/view/theme/bootsmy/template/product/gameserver/fancySelect.css">
 <link rel="stylesheet" media="screen, projection" href="http://story-shop.ru/catalog/view/theme/bootsmy/template/product/gameserver/index.css">
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
<script src="http://story-shop.ru/catalog/view/theme/bootsmy/template/product/gameserver/fancySelect.js"></script>

        <script>
            $(document).ready(function() {
                $('#basic-usage-demo').fancySelect();

                // Boilerplate
                var repoName = 'fancyselect'

                $.get('https://api.github.com/repos/octopuscreative/' + repoName, function(repo) {
                    var el = $('#top').find('.repo');

                    el.find('.stars').text(repo.watchers_count);
                    el.find('.forks').text(repo.forks_count);
                });

                var menu = $('#top').find('menu');

                function positionMenuArrow() {
                    var current = menu.find('.current');

                    menu.find('.arrow').css('left', current.offset().left + (current.outerWidth() / 2));
                }

                $(window).on('resize', positionMenuArrow);

                menu.on('click', 'a', function(e) {
                    var el = $(this),
                        href = el.attr('href'),
                        currentSection = $('#main').find('.current');

                    e.preventDefault();

                    menu.find('.current').removeClass('current');

                    el.addClass('current');

                    positionMenuArrow();

                    if (currentSection.length) {
                        currentSection.fadeOut(300).promise().done(function() {
                            $(href).addClass('current').fadeIn(300);
                        });
                    } else {
                        $(href).addClass('current').fadeIn(300);
                    }
                });

                menu.find('a:first').trigger('click')
            });
        </script>
	</head>